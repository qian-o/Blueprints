<Page x:Class="WinUI.Example.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:bp="using:Blueprints.WinUI"
      xmlns:vm="using:WinUI.Example.ViewModels">
    <Page.DataContext>
        <vm:MainViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Command="{Binding ConnectNodesCommand}"
                Content="Connect Nodes" />

        <bp:BlueprintEditor Grid.Row="1"
                            Margin="0,10,0,0"
                            Elements="{Binding Nodes}" />
    </Grid>
</Page>
